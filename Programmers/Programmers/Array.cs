namespace Programmers;

public static class Array
{
    // 배열에서 Index를 찾는 함수
    // Array.IndexOf
    
    // 배열을 자르기
    // Skip 은 배열의 앞에서부터 start 개수 만큼 건너띔
    // Take 는 Skip 이후의 배열에서 n개수만큼 요소를 가져옴
    // numbers.Skip(num1).Take(num2 - num1 + 1).ToArray();
    
    // 배열의 정렬
    // Array.Sort를 사용하면 추가적인 메모리할당 없이 정렬이 가능함
}